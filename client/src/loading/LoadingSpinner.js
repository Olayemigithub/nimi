import React from 'react';
import './loadingSpinner.css'; // Add necessary CSS for spinner

const LoadingSpinner = () => (
    <div className="spinner">
        <div className="loader">Loading...</div>
    </div>
);

export default LoadingSpinner;
